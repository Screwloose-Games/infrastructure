# tools/

Developer and operational tooling — CLIs, scripts, code generators, migration
and deployment helpers.

Each subdirectory is its own pnpm workspace with its own `package.json`. Tools
are run by people or CI rather than deployed as part of a product.
