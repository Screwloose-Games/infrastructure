# Changesets

Version and changelog management for the workspaces in this monorepo.

Run `pnpm changeset` to describe a change, `pnpm version-packages` to apply
pending changesets to package versions, and `pnpm release` to build and
publish. See the [docs](https://github.com/changesets/changesets) for details.
