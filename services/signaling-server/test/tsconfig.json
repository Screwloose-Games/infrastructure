{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // The Workers test runtime uses bundler-style module resolution.
    "moduleResolution": "bundler",

    // Provides Worker, Durable Object, and test-runtime types.
    "types": ["@cloudflare/vitest-pool-workers/types"]
  },
  "include": ["./**/*.ts", "../worker-configuration.d.ts"]
}
