{
  "name": "@repo/signaling-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WebRTC signaling relay — Cloudflare Worker with one Durable Object per session code",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "test": "vitest run",
    "check-types": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.2",
    "@cloudflare/workers-types": "^5.20260804.1",
    "@repo/typescript-config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.119.0"
  }
}
